<input class="input{{#if className}} {{className}} {{/if}}{{#if readonly}} input_readonly{{/if}}"
    {{#if type}}type="{{type}}" {{/if}}
       name="{{name}}"
    {{#if value}}value="{{value}}" {{/if}}
    {{#if type}}placeholder="{{placeholder}}" {{/if}}
       autocomplete="off">